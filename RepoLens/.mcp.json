{
  "mcpServers": {
    "codex": {
      "command": "docker",
      "args": [
        "compose",
        "run",
        "--rm",
        "-T",
        "api",
        "python",
        "-m",
        "app.mcp_server"
      ]
    }
  }
}
