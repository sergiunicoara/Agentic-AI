# Sample Repo

A tiny fixture repo used to test the ingestion pipeline. Not a real project.

## Setup

### Installation

Nothing to install — this exists purely for tests.

### Configuration

No configuration needed.

## Usage

Import `pkg.models` and call `create_user`.
