Ran the contact list through the merge/cleanup process and there are still
duplicate entries afterward. Same person, same email address, just typed with
different capitalization in each record (one all lowercase, one with a
capital letter). I'd expect the cleanup step to catch that and only keep one
of them.
