apiVersion: v1
kind: Service
metadata:
  name: {{ include "smartops.fullname" . }}-api
  labels:
    {{- include "smartops.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smartops.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
