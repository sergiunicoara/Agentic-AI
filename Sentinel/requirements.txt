google-adk
google-cloud-aiplatform
google-genai
pydantic>=2.0
# fastmcp/mcp/semgrep are pinned together deliberately: semgrep pins
# mcp==1.23.3 exactly, and fastmcp>=2.13.3 (and all of 3.x) requires
# mcp>=1.24.0 — those ranges cannot both be satisfied. fastmcp==2.13.0 is
# the latest version whose mcp range (>=1.17.0,<2.0.0) still includes
# 1.23.3. Do not bump fastmcp or mcp here without re-checking this.
fastmcp==2.13.0
mcp==1.23.3
fastapi
uvicorn
aiofiles
bandit
ruff
pip-audit
semgrep==1.168.0
pytest
